plugins {
    id 'application'
}

//application {
    mainClassName = "net.akehurst.kaf.sample.Main"
//}

// need this so that the gradle application plugin can find the kotlin lib
dependencies {
    runtime project(path:':application.desktop.jvm', configuration:'jvm8RuntimeElements')
}